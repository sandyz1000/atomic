[package]
name = "docker-task"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "docker-task"
path = "src/main.rs"

# Isolate from root workspace — examples/* is excluded
[workspace]

[dependencies]
serde_json = "1.0"
