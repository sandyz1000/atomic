[package]
name = "rust_wasm_task"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
opt-level = "s"
panic = "abort"
codegen-units = 1
strip = true

[workspace]
