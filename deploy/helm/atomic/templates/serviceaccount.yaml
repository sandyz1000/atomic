{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "atomic.serviceAccountName" . }}
  labels:
    {{- include "atomic.labels" . | nindent 4 }}
{{- end }}
